server:
  port: 8080
spring:
  application:
    name: banking-backend
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/bankingdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:mamamia5}
  sql:
    init:
      mode: always
